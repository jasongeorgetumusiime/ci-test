[tox]
; envlist = py39,formatting
envlist = py39


[gh-actions]
python =
    3.9: py39,formatting

[testenv]
deps = pytest
       pytest-cov
       coveralls
       -rrequirements.txt

commands =  
    pytest --cov-config .coveragerc --cov app app/tests
    - coveralls


; [testenv:formatting]
; commands = 
;     isort --check --diff app
; deps =
;     black
;     isort